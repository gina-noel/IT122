<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<header>
</header>
<body>
<main>
<h1>Yoga Poses</h1>

<div class="container">
     <img src="images/yoga.jpg" class="homePage" alt="home page image" />
     <div class="centered">
    <% poses.forEach(function(pose) { %>
        <ul>
            <li><a href="detail?name=<%= pose.name %>"> <%= pose.name %></a></li>
        </ul>
    <% }); %>
    <div>
</div>

</body>
</main>
</html>